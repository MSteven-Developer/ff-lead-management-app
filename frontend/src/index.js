import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App'; // Adjust this import to your main App component

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <App />
);
